# ============================================================================
# Impulsar Frontend - Environment Variables Template
# ============================================================================
# INSTRUCTIONS:
# 1. For LOCAL development:
#    - Copy this file to .env.local: cp .env.example .env.local
#    - Fill in your actual values
#    - .env.local is in .gitignore (safe from git commits)
#
# 2. For VERCEL deployment:
#    - Add these variables in Vercel dashboard: Project Settings → Environment Variables
#    - Vercel uses these values during build and runtime
#    - Each environment (development/preview/production) can have different values
#
# 3. NEVER commit .env.local to git (it's in .gitignore)
# ============================================================================

# ============================================================================
# Backend API Configuration
# ============================================================================
# URL where frontend sends API requests
# Local development: http://localhost:3001
# Production: https://api.impulsar.gov.ar (ajustar según dominio real)
NEXT_PUBLIC_API_URL=http://localhost:3001

# ============================================================================
# Supabase Configuration (Frontend - Public Keys)
# ============================================================================
# Supabase project URL
# Get from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# Supabase Anon Key (public, safe to expose in browser)
# Get from: https://app.supabase.com/project/_/settings/api (under "Project API keys" → "anon public")
# This key respects Row Level Security policies - safe for frontend
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Required: Service role key (backend services only, has full database access)
# Get from: https://app.supabase.com/project/_/settings/api-keys/legacy (under "Service role")
# WARNING: This key bypasses Row Level Security - keep it SECRET
# IMPORTANT: Also add this to Vercel as SUPABASE_URL (same value as NEXT_PUBLIC_SUPABASE_URL)
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# ============================================================================
# CRITICAL SECURITY NOTES
# ============================================================================
# ✅ NEXT_PUBLIC_* variables:
#    - Are EXPOSED to the browser (compiled into JavaScript bundle)
#    - Should ONLY contain public information (API URLs, public keys)
#    - NEVER put secrets here (JWT_SECRET, SUPABASE_SERVICE_ROLE_KEY, etc.)
#
# ❌ DO NOT use NEXT_PUBLIC_* for:
#    - SUPABASE_SERVICE_ROLE_KEY (backend only)
#    - JWT_SECRET (backend only)
#    - STELLAR_OPERATIONAL_SECRET_KEY (backend only)
#    - Any private keys or secrets
#
# 🔒 For server-side secrets (Server Components/API routes):
#    - Add them in Vercel WITHOUT the NEXT_PUBLIC_ prefix
#    - They will ONLY be available in server-side code
#    - Never accessible in client-side JavaScript
# ============================================================================

# ============================================================================
# ¿Dónde obtener los valores de Supabase?
# ============================================================================
# 1. Ve a https://app.supabase.com
# 2. Selecciona tu proyecto (o crea uno nuevo: "New Project")
# 3. Navega a: Settings (⚙️ icono izquierdo) → API
# 4. Copia los valores:
#
#    NEXT_PUBLIC_SUPABASE_URL:
#    - Sección: "Project URL"
#    - Formato: https://xxxxxxxxxxxxx.supabase.co
#    - Ejemplo: https://abcdefghijklmnop.supabase.co
#
#    NEXT_PUBLIC_SUPABASE_ANON_KEY:
#    - Sección: "Project API keys" → "anon public"
#    - Formato: eyJhbGci... (token JWT muy largo, ~200+ caracteres)
#    - ⚠️ Es una clave PÚBLICA - respeta Row Level Security
#    - ✅ Seguro exponerla en frontend
#
# 5. NUNCA uses "service_role" key en NEXT_PUBLIC_* variables
#    - service_role bypasses Row Level Security
#    - Solo debe usarse en backend (transfer-service, auth-service)
# ============================================================================

# ============================================================================
# Crossmint Configuration
# ============================================================================
# Crossmint Client ID for web integration
# Check all supported chains: https://docs.crossmint.com/introduction/supported-chains
# Get from: https://www.crossmint.io/dashboard/settings
NEXT_PUBLIC_CROSSMINT_API_KEY=your_client_side_key_here
NEXT_PUBLIC_CHAIN=stellar-testnet_or_stellar-mainnet

# ============================================================================
# Stellar Blockchain Configuration
# ============================================================================
# CER Oracle Smart Contract ID (deployed on Stellar Testnet/Mainnet)
# This contract stores the official CER value signed by BCRA
# Get this from your contract deployment output
CER_ORACLE_CONTRACT_ID=CCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCC

# Stellar Soroban RPC endpoint
# Testnet: https://soroban-testnet.stellar.org
# Mainnet: https://soroban-mainnet.stellar.org (when ready)
STELLAR_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Stellar Network Passphrase
# Testnet: Test SDF Network ; September 2015
# Mainnet: Public Global Stellar Network ; September 2015
STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015